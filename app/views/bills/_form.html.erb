<!DOCTYPE html>
<html>
<head>
  <style>

  .formulario-completo{
    width: 400px;
    padding: 40px;
    border: 2px solid #ccc;
    border-radius: 20px;
    font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
    background-color: #edf2ff;
    margin-left: 700px;
    margin-top: 50px;
  }


  .titulo{
    margin: 20px 0;
    font-size: 18px;
}

  .campo-input {
    border-radius: 10px;
    padding: 6px;
    border:2px solid #ccc;
    margin-bottom: 8px;
}

  .botao-submit{
    background-color: #304ffe;
    color: white;
    padding: 10px 12px;
    margin-top: 15px;
    font-size: 16px;
    border: none;
    border-radius: 10px;
   }

  </style>
</head>

<body class="formulario-completo">
<div> 
<%= form_with(model: bill) do |form| %>
  <% if bill.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bill.errors.count, "error") %> prohibited this bill from being saved:</h2>

      <ul>
        <% bill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="titulo">
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value, class: "campo-input"  %>
  </div>

  <div class="titulo">
    <%= form.label :due_date, style: "display: block" %>
    <%= form.date_field :due_date, class: "campo-input"  %>
  </div>

  <div class="titulo">
    <%= form.label :enrollment_id, style: "display: block" %>
    <%= form.text_field :enrollment_id, class: "campo-input"  %>
  </div>

  <div class="titulo">
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status, class: "campo-input"  %>
  </div>

  <b class="texto-confirmacao">Antes de seguir, confira se as informações estão corretas, se sim, clique em "Criar".</b>
  <div>
    <%= form.submit class: "botao-submit"%>
  </div>
<% end %>
